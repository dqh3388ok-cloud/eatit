import { PageShell } from "@/pages/page-shell";

export function ConfigPage(): JSX.Element {
  return (
    <PageShell
      title="Interview Config"
      description="Placeholder route for style, direction, and duration configuration."
      routePath="/config"
    />
  );
}
