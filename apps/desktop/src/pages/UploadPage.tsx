import { PageShell } from "@/pages/page-shell";

export function UploadPage(): JSX.Element {
  return (
    <PageShell
      title="Upload & Analysis"
      description="Placeholder route for resume and JD upload plus joint analysis."
      routePath="/upload"
    />
  );
}
