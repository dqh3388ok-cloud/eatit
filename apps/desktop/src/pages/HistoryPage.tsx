import { PageShell } from "@/pages/page-shell";

export function HistoryPage(): JSX.Element {
  return (
    <PageShell
      title="Interview History"
      description="Placeholder route for historical sessions and report status."
      routePath="/history"
    />
  );
}
